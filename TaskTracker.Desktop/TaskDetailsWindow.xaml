<Window x:Class="TaskTracker.Desktop.TaskDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Изменить задачу" Height="450" Width="500"
        Background="White" FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Заголовок" FontWeight="SemiBold" Foreground="#4A5568"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Height="32" Margin="0,5,0,15" Padding="8"/>

        <TextBlock Text="Описание" FontWeight="SemiBold" Foreground="#4A5568" Grid.Row="2"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="2" 
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Margin="0,25,0,15" Padding="8" Height="80"/>

        <TextBlock Text="Срок выполнения:" FontWeight="SemiBold" Grid.Row="3" Margin="0,0,0,5"/>
        <DatePicker x:Name="DueDateBox" Grid.Row="4" Margin="0,0,0,15"/>

        <TextBlock Text="Матрица Эйзенхауэра:" FontWeight="SemiBold" Grid.Row="5" Margin="0,0,0,5"/>
        <StackPanel Orientation="Horizontal" Grid.Row="6" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Срочно?" FontWeight="SemiBold"/>
                <RadioButton x:Name="UrgentYes" Content="Да" GroupName="Urgent" Margin="0,5,0,5"/>
                <RadioButton x:Name="UrgentNo" Content="Нет" GroupName="Urgent" Margin="0,5,0,5"/>
            </StackPanel>
            <StackPanel Margin="20,0,0,0">
                <TextBlock Text="Важно?" FontWeight="SemiBold"/>
                <RadioButton x:Name="ImportantYes" Content="Да" GroupName="Important" Margin="0,5,0,5"/>
                <RadioButton x:Name="ImportantNo" Content="Нет" GroupName="Important" Margin="0,5,0,5"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="SaveButton_Click" 
                    Background="#38A169" Foreground="White" 
                    Width="90" Height="32" Margin="0,0,10,0" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Удалить" Click="DeleteButton_Click" 
                    Background="#E53E3E" Foreground="White" 
                    Width="90" Height="32" Margin="0,0,10,0" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Закрыть" Click="CloseButton_Click" 
                    Background="#E2E8F0" Foreground="#2D3748" 
                    Width="90" Height="32" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>