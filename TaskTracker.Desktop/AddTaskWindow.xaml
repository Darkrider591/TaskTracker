<Window x:Class="TaskTracker.Desktop.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Add task" Height="400" Width="500" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Title:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,0,0,10"/>

        <!-- Описание -->
        <TextBlock Text="Description:" FontWeight="Bold" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="2" 
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Margin="0,25,0,10" TextChanged="DescriptionBox_TextChanged"/>

        <!-- Срок выполнения -->
        <TextBlock Text="Deadline:" FontWeight="Bold" Grid.Row="4" Margin="0,0,0,5"/>
        <DatePicker x:Name="DueDateBox" Grid.Row="5" Margin="0,0,0,10" SelectedDate="{x:Static sys:DateTime.Today}"/>

        <!-- Матрица Эйзенхауэра -->
        <TextBlock Text="Eisenhower Matrix:" FontWeight="Bold" Grid.Row="6" Margin="0,0,0,5"/>

        <StackPanel Orientation="Horizontal" Grid.Row="7" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Urgently?" FontWeight="SemiBold"/>
                <RadioButton x:Name="UrgentYes" Content="Yes" GroupName="Urgent" IsChecked="True" Margin="0,5,0,5"/>
                <RadioButton x:Name="UrgentNo" Content="No" GroupName="Urgent" Margin="0,5,0,5"/>
            </StackPanel>

            <StackPanel Margin="20,0,0,0">
                <TextBlock Text="Important?" FontWeight="SemiBold"/>
                <RadioButton x:Name="ImportantYes" Content="Yes" GroupName="Important" IsChecked="True" Margin="0,5,0,5"/>
                <RadioButton x:Name="ImportantNo" Content="No" GroupName="Important" Margin="0,5,0,5"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="8" Margin="0,30,0,0">
            <Button Content="Add" Click="OnAddClick" Width="50" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="OnCancelClick" Width="50"/>
        </StackPanel>
    </Grid>
</Window>